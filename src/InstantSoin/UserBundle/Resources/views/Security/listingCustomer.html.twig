{% extends 'UserBundle:Admin:admin.html.twig' %}
{% block body %}

    <h3>Gestion des comptes client</h3>
    <div>
        <a href="new_account" class="btn btn-success actionButton">Ajouter un compte client</a>
    </div>
    <br><br>
        <table class="table table-hover modifTable" >
            <thead>
                <tr>
                    <th>Nom et prénom du client</th>
                    <th>Nom utilisateur</th>
                    <th>Action</th>
                 </tr>
            </thead>
            <tbody>
                {% for user in users %}
                    <tr>
                        <td>
                            {{ users[ loop.index0 ].prenom }} {{ users[ loop.index0 ].nom }}
                        </td>
                        <td>
                            {{ users[ loop.index0 ].username }}
                        </td>
                        <td>
                            <a href="{{ path( 'updateCustomer', {'id' : users[ loop.index0 ].id } ) }}" class="btn btn-primary actionButton">Modifier</a><br><br>
                            <a href="{{ path( 'deleteCustomer', {'id' : users[ loop.index0 ].id } ) }}" class="btn btn-danger actionButton">Supprimer</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
	
{% endblock %}