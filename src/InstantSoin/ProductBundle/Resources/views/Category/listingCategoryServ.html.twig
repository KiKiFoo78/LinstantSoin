{% extends 'UserBundle:Admin:admin.html.twig' %}
{% block body %}

    <h3>Gestion des catégories de service</h3>
    <div>
        <a href="createCategoryServ" class="btn btn-success actionButton">Ajouter une catégorie</a>
    </div>
    <br><br>
    <section>
        <table class="table table-hover modifTable" >
            <thead>
                <tr>
                    <th>Intitulé</th>
                    <th>Image liée</th>
                    <th>Description</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                {% for categServ in categoriesServ %}
                    <tr>
                        <td>
                            {{ categoriesServ[ loop.index0 ].intitule }}
                        </td>
                        <td>
                            <img src="{{ asset(categoriesServ[ loop.index0 ].urlimage) }}" class="miniImgProd">
                        </td>
                        <td>
                            {{ categoriesServ[ loop.index0 ].description }}
                        </td>
                        <td>
                                <a href="{{ path( 'updateCategoryServ', {'id' : categoriesServ[ loop.index0 ].id } ) }}" class="btn btn-primary actionButton">Modifier</a><br><br>                            
                                <a href="{{ path( 'deleteCategoryServ', {'id' : categoriesServ[ loop.index0 ].id } ) }}" class="btn btn-danger actionButton">Supprimer</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </section>
{% endblock %}